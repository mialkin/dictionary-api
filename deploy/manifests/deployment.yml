apiVersion: apps/v1
kind: Deployment
metadata:
  name: dictionary-api
  namespace: mialkin
  labels:
    app: dictionary-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: dictionary-api
  template:
    metadata:
      namespace: mialkin
      labels:
        app: dictionary-api
    spec:
      containers:
        - name: dictionary-api
          imagePullPolicy: Always
          image: mialkin/dictionary-api:latest
          ports:
            - containerPort: 80
